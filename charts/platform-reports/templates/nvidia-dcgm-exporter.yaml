{{- if .Values.nvidiaDCGMExporter.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "platformReports.nvidiaDCGMExporter.fullname" . }}
  labels: {{ include "platformReports.labels.standard" . | nindent 4 }}
    service: nvidia-dcgm-exporter
spec:
  selector: {{ toYaml .Values.nvidiaDCGMExporter.service.selector | nindent 4 }}
  ports: {{ toYaml .Values.nvidiaDCGMExporter.service.ports | nindent 4 }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "platformReports.nvidiaDCGMExporter.fullname" . }}
  labels: {{ include "platformReports.labels.standard" . | nindent 4 }}
    service: nvidia-dcgm-exporter
spec:
  selector:
    matchLabels:
      app: {{ include "platformReports.name" . }}
      release: {{ .Release.Name | quote }}
      service: nvidia-dcgm-exporter
  endpoints:
    - port: metrics
      path: /metrics
      honorLabels: true
      interval: 15s
      relabelings:
        - sourceLabels:
            - __metrics_path__
          targetLabel: metrics_path
{{- end }}
