{{- if .Values.grafanaProxy.ingress }}
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ include "platformReports.grafanaProxy.fullname" . }}
  labels: {{ include "platformReports.labels.standard" . | nindent 4 }}
  annotations:
    ingress.kubernetes.io/auth-trust-headers: "true"
    ingress.kubernetes.io/auth-type: forward
    ingress.kubernetes.io/auth-url: {{ .Values.platform.ingressAuthUrl }}/oauth/authorize
    kubernetes.io/ingress.class: traefik
    traefik.frontend.rule.type: PathPrefix
{{- if .Values.grafanaProxy.ingress.extraAnnotations }}
{{ toYaml .Values.grafanaProxy.ingress.extraAnnotations | indent 4 }}
{{- end }}
spec:
  rules:
    {{- range .Values.grafanaProxy.ingress.hosts }}
    - host: {{ . | quote }}
      http:
        paths:
          - backend:
              serviceName: {{ include "platformReports.grafanaProxy.fullname" $ }}
              servicePort: http
            path: /api/dashboards
    - host: {{ . | quote }}
      http:
        paths:
          - backend:
              serviceName: {{ include "platformReports.grafanaProxy.fullname" $ }}
              servicePort: http
            path: /ping
    - host: {{ . | quote }}
      http:
        paths:
          - backend:
              serviceName: {{ include "platformReports.grafana.fullname" $ }}
              servicePort: 80
            path: /
    {{- end }}
{{- end }}
